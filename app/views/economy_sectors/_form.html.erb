<%= form_with(model: economy_sector, local: true) do |form| %>
  <% if economy_sector.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(economy_sector.errors.count, "error") %> prohibited this economy_sector from being saved:</h2>

      <ul>
      <% economy_sector.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
