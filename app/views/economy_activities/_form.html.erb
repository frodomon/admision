<%= form_with(model: economy_activity, local: true) do |form| %>
  <% if economy_activity.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(economy_activity.errors.count, "error") %> prohibited this economy_activity from being saved:</h2>

      <ul>
      <% economy_activity.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :ciiu %>
    <%= form.text_field :ciiu %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
