
<% provide(:title, "AELUCOOP - Admisión de Socios | Iniciar Sesión") %>
  <section id="forms" class="centered-form-flex">
    <div class="middle-form m-b">
      <h2>Log in</h2>
      <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
        <div class="form-group">
          <%= f.email_field :email, placeholder: 'Correo Electrónico', class: 'form-control', autofocus: true %>
        </div>
        <div class="form-group">
          <%= f.password_field :password, placeholder: 'Contraseña',class: 'form-control', autocomplete: "off" %>
        </div>

        <div class="actions">
          <%= f.submit "Log in", class: 'btn btn-primary full-width m-b' %>
        </div>
      <% end %>
      <%= render "devise/shared/links" %>
    </div>
  </section>
